This is minimal pytorch project that trains a neural network to learn the addition of two numbers

Dataset: orderd pairs (a,b),
Model : only single liner layer,
Loss : Mean squared error,
Epochs : 200 
